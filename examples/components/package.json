{
  "private": true,
  "scripts": {
    "build": "pulp browserify --optimise --to dist/example.js"
  },
  "dependencies": {
    "virtual-dom": "^2.1.1"
  },
  "devDependencies": {
    "pulp": "^8.1.0",
    "purescript": "^0.7.6"
  }
}

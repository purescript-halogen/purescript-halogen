{
  "private": true,
  "scripts": {
    "build": "pulp browserify --optimise --to dist/example.js"
  },
  "dependencies": {
    "virtual-dom": "^2.1.1"
  },
  "devDependencies": {
    "pulp": "^9.0.1",
    "purescript": "^0.9.2"
  }
}

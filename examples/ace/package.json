{
  "private": true,
  "scripts": {
    "build": "pulp browserify --main Example.Ace.Main --optimise --to dist/example.js"
  },
  "devDependencies": {
    "pulp": "^11.0.0",
    "purescript": "^0.11.7"
  }
}

{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "eslint src && pulp build -- --censor-lib --strict",
    "test": "pulp build -I test -- --censor-lib --strict",
    "example-ace": "bower link && cd examples/ace && bower link purescript-halogen && bower install && npm run build",
    "example-components": "bower link && cd examples/components && bower link purescript-halogen && bower install && npm run build",
    "example-counter": "bower link && cd examples/counter && bower link purescript-halogen && bower install && npm run build",
    "example-effects-aff-ajax": "bower link && cd examples/effects-aff-ajax && bower link purescript-halogen && bower install && npm run build",
    "example-effects-eff-random": "bower link && cd examples/effects-eff-random && bower link purescript-halogen && bower install && npm run build",
    "example-interpret": "bower link && cd examples/interpret && bower link purescript-halogen && bower install && npm run build",
    "example-intro": "bower link && cd examples/intro && bower link purescript-halogen && bower install && npm run build",
    "example-keyboard-input": "bower link && cd examples/keyboard-input && bower link purescript-halogen && bower install && npm run build",
    "example-lifecycle": "bower link && cd examples/lifecycle && bower link purescript-halogen && bower install && npm run build",
    "example-multi-component": "bower link && cd examples/multi-component && bower link purescript-halogen && bower install && npm run build",
    "example-todo": "bower link && cd examples/todo && bower link purescript-halogen && bower install && npm run build"
  },
  "dependencies": {
    "virtual-dom": "^2.1.1"
  },
  "devDependencies": {
    "eslint": "^3.10.2",
    "pulp": "^10.0.0",
    "purescript": "^0.10.2",
    "purescript-psa": "^0.3.9",
    "rimraf": "^2.5.4"
  }
}
